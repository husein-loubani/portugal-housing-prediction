# Pinned to the exact training environment so the model behaves identically
# in the container (no version drift between training and serving).
fastapi==0.136.0
uvicorn[standard]==0.44.0
joblib==1.5.3
pandas==2.3.3
numpy==2.2.6
scikit-learn==1.7.2
lightgbm==4.6.0
pydantic==2.13.2
